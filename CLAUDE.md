# tariq-website — Website (Tariq Technologies)

This repo is the **Website** stream: the marketing site and its design system. Stay
scoped to this repo's own domain — don't read or pull in other repos' content unless the
user explicitly asks.

## If you need company/coordination context
For business context or current company priorities relevant to the website specifically,
read `~/projects/Tariq-dz/tariq-hub/docs/01-company-context.md` and `04-roadmap.md` —
skip sections about other departments unless a task explicitly touches them.

## Crossing into another repo
If a task needs information from, or an update to, another repo (`tariq-app`,
`tariq-hardware`, `tariq-social`, `tariq-hub`) — **ask the user first**, don't do it
silently.

`tariq-hub` is coordination tooling only — it never contains product/technical
documentation about the website. Don't look there for implementation details, and don't
put implementation details there.
